<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Record Rules - loaded after models are created -->
        <record id="document_template_company_rule" model="ir.rule">
            <field name="name">Document Template: Multi-Company</field>
            <field name="model_id" ref="model_sm_document_template"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>
    </data>
</odoo>
